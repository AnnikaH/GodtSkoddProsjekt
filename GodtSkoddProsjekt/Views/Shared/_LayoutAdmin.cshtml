<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>
    <!-- jQuery: -->
    <script src="~/Scripts/jquery-1.9.1.js"></script>
    <script src="~/Scripts/jquery.validate.js"></script>
    <script src="~/Scripts/jquery.validate.unobtrusive.js"></script>
    <!-- Bootstrap: -->
    <link href="~/Content/bootstrap.css" rel="stylesheet" />
    <link href="~/Content/bootstrap-theme.css" rel="stylesheet" />
    <script src="~/Scripts/bootstrap.js"></script>
    <!--- Our stylesheet-->
    <link href="~/Content/Style.css" rel="stylesheet" />
    <!-- Ajax: -->
    <script src="~/Scripts/jquery.unobtrusive-ajax.js"></script>
    <script type="text/javascript">

        /* Copied from _Layout.cshtml:

        function checkLogin() {
            var username = $('#logginnbrukernavn').val();
            var password = $('#logginnpassord').val();

            $.getJSON("/Home/Login", { username: username, password: password }, function (loginUserData) {
                // only enters here if success

                //window.location.reload();
                // Because of /Admin/Index (need to change location to update navbar when logging in from here):
                window.location = "/Home/Index";

                $('#logginntilbakemelding').html("");
            });

            // Is there a better solution?
            if (document.getElementById('minSideLi') == null) {
                $('#logginntilbakemelding').html("Feil brukernavn eller passord!");
            }
        }
        */

        function deleteAdminUser(id) {
            if (NaN(id))
                return;

            $.getJSON("/ADMINMain/DeleteAdminUser", { id: id }, function (deleteOkData) {
                // only enters here if success

                // deleteOkData contains "true" or "false"

                window.location = "/ADMINMain/AdminAdminUsers";

                //$('#logginntilbakemelding').html("");
            });

            //$('#logginntilbakemelding').html("Feil brukernavn eller passord!");
        }

        function deleteUser(id) {
            if (NaN(id))
                return;

            $.getJSON("/ADMINMain/DeleteUser", { id: id }, function (deleteOkData) {
                // only enters here if success

                // deleteOkData contains "true" or "false"

                window.location = "/ADMINMain/AdminCustomers";

                //$('#logginntilbakemelding').html("");
            });

            //$('#logginntilbakemelding').html("Feil brukernavn eller passord!");
        }
    </script>
</head>
<body>
    <nav class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                @Html.ActionLink("Godt Skodd", "Index", "ADMINMain", new { id = "" }, new { @class = "navbar-brand" })
                
            </div>

            <!-- TODO: Legge inn link til hoved-admin-siden (ADMINMain/Index.cshtml) + en logg ut-knapp +
                vise hvem som er logget inn (?) -->
            
        </div>
    </nav>
        <div>
        </div>

        <div class="container body-content">
            @RenderBody()
            <hr />
            <footer>
                <p>&copy; @DateTime.Now.Year - Godt Skodd</p>
            </footer>
        </div>
</body>
</html>